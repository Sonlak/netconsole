### 09:00 -- Switch auto-collect cadence raised to 15 min

- User complained SSH login từ backend vẫn dày đặc, dù managed-check
  đã chuyển sang TCP-only. Tần suất collect MAC/ARP/Interfaces/Config
  còn lại vẫn rát (120s/120s/120s/300s).
- User: "tăng lên cho tôi đi, 30 phút đi" — đã hỏi lại, user chốt 15
  phút (900s). Managed check giữ nguyên như cũ (TCP-only probe).
- Sửa `backend/src/index.ts` default + `backend/.env.example`:
    MAC_COLLECT=900, ARP_COLLECT=900, INTERFACES_COLLECT=900,
    CONFIG_COLLECT=900 (đều có thể override qua env).
- Tần suất cũ đẩy ~1 SSH login mỗi 2 phút/device (60 thiết bị = ~30 SSH
  login/giờ). Mới: ~1 mỗi 15 phút/device (~240 SSH login/giờ vẫn nhiều
  nhưng giảm 7.5×). User có nút Collect-now ở DeviceDetail /
  NetworkTables / Logs để chạy thủ công khi cần.
- Các `schedule*Collection` còn `Math.max(N, 60)` ở backend — sàn 60s,
  không ảnh hưởng default 900s.
- Build `tsc` OK. Commit `+2 -2`, sẽ push → Deploy Actions.
- Snapshot `.env.example` đã đổi nhưng `.env` thật trên VPS (từ
  `docker-compose.app.yml`) không thay đổi tự động — user cần set
  override trong compose nếu muốn cứng 900s trên VPS; nếu không thì
  default trong `index.ts` mới sẽ áp dụng sau khi container rebuild
  (code default đã đổi).
