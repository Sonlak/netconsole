# 2026-09-19

## 11:40 — fix(backend/terminal): add ETM MACs + GCM ciphers for IOS-XE 17.x

**Issue (user-reported 2026-09-19 11:40 ICT):**
- LAB-F3-AS-02 (10.10.20.212, Cisco IOS-XE 17.16.1a Virtual XE Software) terminal
  failed with `Handshake failed: no matching C->S MAC`. Backend logs showed
  two failed attempts: `Session f659173b...: SSH error: Handshake failed:
  no matching C->S MAC`.

**Root cause:** ssh2's default algorithm list omits the
`*-etm@openssh.com` MAC variants (`hmac-sha2-512-etm@openssh.com`,
`hmac-sha2-256-etm@openssh.com`) that Cisco IOS-XE 17.x advertises as
its preferred C->S MAC. Confirmed via paramiko probe on 2026-09-19
against the same device — paramiko selected `hmac-sha2-256-etm@openssh.com`
by default, while ssh2 had no overlap.

Server's full KEXINIT offer (verified):
- KEX: curve25519-sha256@libssh.org, ecdh-sha2-nistp256/384/521,
  diffie-hellman-group16-sha512, diffie-hellman-group-exchange-sha256,
  diffie-hellman-group14-sha256
- MAC: hmac-sha2-256, hmac-sha2-512, hmac-sha2-256-etm@openssh.com,
  hmac-sha2-512-etm@openssh.com, hmac-sha1, hmac-md5, hmac-sha1-96,
  hmac-md5-96
- Cipher: aes{128,192,256}-ctr, aes{128,192,256}-cbc, 3des-cbc,
  aes128-gcm@openssh.com, aes256-gcm@openssh.com
- HostKey: ssh-ed25519, ecdsa-sha2-nistp*, rsa-sha2-{256,512}

ssh2 only had the non-ETM MACs + non-GCM ciphers — no match.

**Fix:** add `hmac-sha2-512-etm@openssh.com`, `hmac-sha2-256-etm@openssh.com`,
`hmac-sha1-etm@openssh.com`, `hmac-md5-96` to the `algorithms.hmac` list,
and `aes128-gcm@openssh.com`, `aes256-gcm@openssh.com` to `algorithms.cipher`.
ETM variants listed first so they're the KEXINIT preference (no extra
round-trip). Existing non-ETM algos still listed for older IOS peers.

**Files:**
- `backend/src/websocket/terminal.ts` — extend the existing algorithms block

**Worker (paramiko) unaffected** — paramiko's default algorithm list
already includes the ETM variants, which is why the same device's
ARP/MAC collect via `iosxe.py::get_arp` SSH fallback works fine.
This bug was ssh2-only (terminal + the backend's `labSsh.ts` discovery
probe — but `labSsh.ts` is not on the F3-AS-02 hot path today, only
`runIosxeSshApply` for IOS-XE config writes, which isn't enabled for
this device yet).

**Audit of "Junos lag" complaint (same chat):**
- 3 of 5 Junos devices (`LAB-F6-CORE-02` 10.10.20.101, `LAB-F6-DS-01`
  10.10.20.110, `LAB-F6-DS-02` 10.10.20.111) are **physically unreachable
  from the worker container** — TCP probes returned `No route to host`
  on 22/830/80/443. Not a code issue, lab infrastructure problem.
- 2 of 5 (`LAB-F2-AS-01` 10.10.20.221, `LAB-F6-CORE-01` 10.10.20.102)
  are MANAGED with all 4 GET types running fine — every 5/10/15 min,
  5-15 s per job, manual click returns in ~2 s.
- Auto-collect correctly skips the OFFLINE devices (managed_check gate
  is `ping && rest`, both fail for the 3 unreachable devices).
- No FAILED jobs in the last 500 — only SUCCESS rows.
- The "lag / click get → shows later" the user described almost certainly
  maps to the 3 unreachable devices: user opens page, auto-collect never
  fires for them, latest SUCCESS job is from days ago, UI shows stale or
  empty. Clicking "Get" either queues a job that eventually times out
  (worker tries SSH → No route) or is silently dropped if the worker
  classifies the timeout early. None of this is fixable in code — the
  lab containers for F6-CORE-02 / F6-DS-01 / F6-DS-02 need to come back up.

**Things to do next:**
- Tell user which 3 devices are unreachable at network level (lab infra).
- Once those 3 containers are back up, the "Junos lag" complaint
  disappears — no code change required.
- If user wants per-device "reachability banner" in UI (e.g., a red
  tag "Network unreachable" instead of plain "OFFLINE"), backlog it.
- Verify after deploy: re-attempt terminal on 10.10.20.212, expect
  banner to land and shell to open.