# Sessions -- 2026-09-07

### 2026-09-07 09:40 -- Multi-vendor extension plan + device configs (token-budget)

- User asked to plan + record for next agent; couldn't start implementation this session due to token budget.
- Wrote `docs/agents/08-vendor-extension-plan.md` -- full refactor plan: vendor selector `worker/netconsole_worker/vendor.py`, `backends/{base,juniper,eos,iosxe,nxos}.py`, unified `http_pool.py`, 4 vendors' RPC specifics (GET/APPLY/ROLLBACK per task), risk table, file-level change list, rollout order.
- Wrote `docs/agents/09-vendor-device-configs.md` -- copy/paste CLI blocks for EOS (eAPI + syslog), IOS-XE (RESTCONF + NETCONF + syslog), NX-OS (NX-API + syslog), plus security checklist and one-screen cheat sheet.
- Added Active TODO in `AGENTS.md` so the next session finds it. Reminder for next agent:
  1. **FIRST STEP**: refactor `backends/juniper.py` from existing `tasks/registry.py` -- proves zero regression on lab Juniper.
  2. Check lab sim availability: `ceos`, `csr1000v`, `n9kv` images + decide whether to ship `lab/*.clab.yml` for all three in the same PR or as a follow-up.
  3. Decision still open: shared `netconsole/Admin@123` user per-vendor OR per-device credentials (Phase 2, `Device.connectorJson`).
  4. New pip deps in `worker/requirements.txt`: `ncclient`, `textfsm`, `ntc-templates`.
  5. Treat each new vendor as a separate PR (CI per-PR keeps blast radius tight).
- Status: NO code changes this session (token budget). Both docs committed locally; ready for commit+push next session.
- Rollout order reminder (smallest blast radius first): juniper refactor -> EOS -> IOS-XE -> NX-OS -> frontend vendor Select.
