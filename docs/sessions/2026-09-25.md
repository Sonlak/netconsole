### 09:49 -- fix(fabric): Juniper DS ports use LLDP Port ID

- Sửa `worker/netconsole_worker/parsers/show_lldp_neighbors.py` để đọc `lldp-remote-port-id` làm `remotePort`.
- Giữ `lldp-remote-port-description` riêng ở `portDescription`; không còn dùng mô tả port như tên interface.
- Sửa `backend/src/services/fabricTopology.ts` để LLDP `localPort` cập nhật đúng đầu link và loại bỏ remote port dạng số thuần như `525`.
- Case kiểm tra `remotePort=xe-0/0/9`, description giả `ge-0/0/9` trả đúng `xe-0/0/9`.
- Backend TypeScript build pass; 52 backend tests pass; linter không báo lỗi cho hai file sửa.
- Chưa commit, push hoặc deploy production. Cần collect lại `GET_INTERFACES` cho các thiết bị DS sau khi deploy để dữ liệu Job mới được tạo.
