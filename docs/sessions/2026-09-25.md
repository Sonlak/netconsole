### 09:49 -- fix(fabric): Juniper DS ports use LLDP Port ID

- Sửa `worker/netconsole_worker/parsers/show_lldp_neighbors.py` để đọc `lldp-remote-port-id` làm `remotePort`.
- Giữ `lldp-remote-port-description` riêng ở `portDescription`; không còn dùng mô tả port như tên interface.
- Sửa `backend/src/services/fabricTopology.ts` để LLDP `localPort` cập nhật đúng đầu link và loại bỏ remote port dạng số thuần như `525`.
- Case kiểm tra `remotePort=xe-0/0/9`, description giả `ge-0/0/9` trả đúng `xe-0/0/9`.
- Backend TypeScript build pass; 52 backend tests pass; linter không báo lỗi cho hai file sửa.
- Phát hiện nguyên nhân còn sót: EOS parser biến `neighborPort=523/524` thành `Ethernet523/524`, backend tiếp tục đổi thành `et523/et524`; đây là mã port nội bộ của EOS, không phải interface Juniper.
- Sửa `worker/netconsole_worker/backends/eos.py`: chỉ chuyển numeric `localPort` thành `Ethernet<N>`; numeric remote port để rỗng để dùng LLDP reciprocal nếu có.
- Refresh sau deploy xác nhận EOS numeric remote port đã biến mất, nhưng Juniper DS tạo job `SUCCESS` giả với `result.interfaces=[]`, `lldpNeighbors=[]`, `message=No route to host`; job mới này làm topology mất dữ liệu cũ.
- Lỗi còn lại được tái hiện từ output LLDP thực tế: DS Junos local interfaces là `xe-0/0/x`, nhưng description cũ chứa `ge-0/0/x` nên topology hiển thị sai đầu DS.
- Sửa `fabricTopology.ts` thêm map `(device, peer) -> lldp.localPort` và áp dụng sau merge; localPort LLDP của từng thiết bị là nguồn authoritative cho từng đầu link.
- Bản fix tiếp theo thêm fallback `descriptionLocalPorts`: nếu LLDP không trả neighbor, topology vẫn lấy `interfaces[].name` của chính thiết bị làm local port; LLDP vẫn được ưu tiên khi có dữ liệu.
- Đã kiểm tra raw job DS-01/DS-02: interface names thực tế là `xe-0/0/x`, LLDP hiện trả rỗng.

### 11:41 -- feat(arp): trace ARP host to access port

- API ARP ghép MAC trong ARP table với job `GET_MAC` mới nhất trên các access switch.
- Loại các MAC entry nằm trên port uplink đã biết từ fabric topology để tránh trả về trunk của distribute.
- Thêm `endUserDevice` và `endUserPort` vào từng dòng ARP.
- Tab ARP hiển thị thêm cột `End-user device` và `End-user port`; thiếu dữ liệu hiển thị `—`.
- Backend `npm run build` và frontend `npm run build` đều pass.

### 22:20 -- feat(dhcp): map DHCP leases to client ports

- Đổi field ARP từ `endUserDevice`/`endUserPort` sang `clientDevice`/`clientPort`; cột ARP hiển thị Client device và Client port.
- Dùng chung `buildClientLookup` để ghép MAC DHCP với MAC table mới nhất trên access switch, loại trừ uplink theo fabric topology.
- Thêm Client device và Client port vào API/types/bảng DHCP; áp dụng cho dynamic leases và static reservations.
- Backend và frontend `npm run build` pass; lint không báo lỗi backend service. Frontend build có các cảnh báo chunk/dynamic import đã tồn tại.
